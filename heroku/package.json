{
  "name": "internet-remote-controler",
  "main": "index.mjs",
  "scripts": {
    "start": "node index.mjs",
    "deploy": "git push heroku main && npm run execute",
    "start-heroku": "heroku ps:scale web=1",
    "stop-heroku": "heroku ps:scale web=0",
    "log": "heroku logs --tail"
  },
  "dependencies": {
    "net": "^1.0.2",
    "node-fetch": "^3.2.10"
  }
}
